Current verification cycle status: Configured but not executed.

Trivy 0.72.0 was invoked for a filesystem scan. The default vulnerability database mirror timed out; a retry against ghcr.io/aquasecurity/trivy-db failed TLS certificate verification. Docker Engine and Compose are available, but the application image build also failed Docker Hub TLS verification, so no image scan was possible. No CVE or severity count is claimed.

Evidence: reports/security/trivy-before.txt, reports/security/trivy-fs-before-console.txt, reports/security/vulnerability-resolution.md.
