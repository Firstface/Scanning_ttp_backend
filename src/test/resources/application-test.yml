spring:
  datasource:
    url: jdbc:h2:mem:scanning_ttp_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
app:
  auth:
    enabled: false
  rate-limit:
    enabled: false
